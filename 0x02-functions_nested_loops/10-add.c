#include "main.h"
/**
 * add - Entry Point of the Program
 * Desc: adds two integers and returns the result
 * @num: number to add
 * @num2: number to add to num
 * Return: 0 means ran and exited successfully
 */

int add(int num, int num2)
{
	int result;

	result = num + num2;

	return (result);
}
